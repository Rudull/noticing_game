# Noticing Game Backend Dependencies
# Python requirements for the subtitle extraction server and desktop app

# --- Core Runtime (Subtitle Server) ---
# Web framework
Flask>=2.3.0,<3.0.0
# CORS support for cross-origin requests
Flask-CORS>=4.0.0,<5.0.0
# YouTube subtitle extraction (Maintenance script handles auto-updates)
yt-dlp>=2025.12.8

# --- Desktop Application (GUI & tray) ---
# GUI framework: tkinter (usually part of Python, may require 'python3-tk' on Linux)
requests>=2.31.0,<3.0.0
# Underlying library for requests
urllib3>=1.26.16,<3.0.0
# System tray support
pystray>=0.19.5
# Image processing for tray icons
Pillow>=10.4.0
# Windows system integration (Tray, Auto-startup)
pywin32; sys_platform == 'win32'

# --- Development and Testing (Optional) ---
pytest>=7.4.0
pytest-flask>=1.2.0
pytest-cov>=4.1.0

# --- Build and Style (Optional) ---
# For creating standalone executables
pyinstaller>=6.0.0
# Code formatting
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
